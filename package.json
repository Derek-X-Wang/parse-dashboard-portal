{
  "name": "parse-dashboard-portal",
  "version": "1.0.0",
  "description": "portal",
  "main": "index.js",
  "scripts": {
    "start": "parse-dashboard --config parse-dashboard-config.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Derek-X-Wang",
  "license": "ISC",
  "dependencies": {
    "parse-dashboard": "^1.0.22"
  }
}
